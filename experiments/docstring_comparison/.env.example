# Скопируй в .env и заполни своими ключами

# OpenRouter API key (используется и генераторами, и судьёй)
OPENAI_API_KEY=<OPENAI_API_KEY>

# GitHub token — нужен только если генераторы делают git operations (clone/fork)
GIT_TOKEN=<GIT_TOKEN>
